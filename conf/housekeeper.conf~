# Housekeeper configuration file (housekeeper.conf)

config:

  # System settings
  system:
      sudo_password         :   Anonjh140790$dm
      tested_ruby_version   :   2.1.1

  # Email notification settings
  email:
      smtp_server_addr      :   jin.webserversystems.com
      to_email_addr         :   dannydirect90@gmail.com
      from_email_addr       :   housekeeper@daniel-middleton.com

  # Plugins are executed in the below order
  plugins:
      apt_get_update:
          name              :   apt_get_update
          file_path         :   plugins/apt_get_update
          class_name        :   Apt_get_update
          method_name       :   Perform_apt_get_update

      apt_get_upgrade:
          name              :   apt_get_upgrade
          file_path         :   plugins/apt_get_upgrade
          class_name        :   Apt_get_upgrade
          method_name       :   Perform_apt_get_upgrade

      apt_get_dist_upgrade:
          name              :   apt_get_dist_upgrade
          file_path         :   plugins/apt_get_dist_upgrade
          class_name        :   Apt_get_dist_upgrade
          method_name       :   Perform_apt_get_dist_upgrade

      apt_get_autoremove:
          name              :   apt_get_autoremove
          file_path         :   plugins/apt_get_autoremove
          class_name        :   Apt_get_autoremove
          method_name       :   Perform_apt_get_autoremove
